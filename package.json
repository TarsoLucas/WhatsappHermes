{
  "name": "whatsapphermes-master",
  "version": "1.0.0",
  "description": "Sistema de automação de mensagens do WhatsApp - Deploy Railway",
  "main": "Server/main.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node Server/main.js",
    "build": "echo 'Build do React já incluído no repositório'",
    "postinstall": "cd Server && npm install --only=production",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "whatsapp",
    "automation",
    "messaging",
    "railway"
  ],
  "author": "",
  "license": "ISC"
}
